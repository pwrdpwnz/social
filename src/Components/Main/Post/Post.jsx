import React from 'react';
import PostStyles from './Post.module.scss';

const Post = () => {
    return (
        <div className={PostStyles.Post}>
             Post1
        </div>
    );
};

export default Post;